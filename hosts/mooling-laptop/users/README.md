# User profile in specific host machine

These `${username}.nix` modules are managed by [home-manager](../../../modules/home/default.nix#L15).

Once the `${username}` and `${hostname}` variables matches, the configurations will be actually applied.
